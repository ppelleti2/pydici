{{- if .Values.celery.beat.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "pydici.fullname" . }}-celery-beat
  labels:
    {{- include "pydici.labels" . | nindent 4 }}
    app.kubernetes.io/component: celery-beat
spec:
  replicas: {{ .Values.celery.beat.replicaCount }}
  selector:
    matchLabels:
      {{- include "pydici.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: celery-beat
  template:
    metadata:
      labels:
        {{- include "pydici.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: celery-beat
        app.kubernetes.io/name: {{ include "pydici.fullname" . }}-celery-beat
    spec:
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
      - name: wait-for-redis
        image: busybox:1.35
        command:
        - /bin/sh
        - -c
        - |
          echo "Waiting for Redis to be ready..."
          until nc -z {{ include "pydici.redisHost" . }} {{ .Values.redis.service.port }}; do
            echo "Redis not ready, sleeping..."
            sleep 2
          done
          echo "Redis is ready!"
      containers:
      - name: celery-beat
        {{- with .Values.securityContext }}
        securityContext:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        image: "{{ .Values.pydici.image.registry }}/{{ .Values.pydici.image.repository }}:{{ .Values.pydici.image.tag }}"
        imagePullPolicy: {{ .Values.pydici.image.pullPolicy }}
        command:
        - celery
        - -A
        - pydici
        - beat
        - --loglevel=info
        - --scheduler=django_celery_beat.schedulers:DatabaseScheduler
        env:
        - name: SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: {{ include "pydici.fullname" . }}-django
              key: secret-key
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ include "pydici.fullname" . }}-mariadb
              key: mariadb-password
        envFrom:
        - configMapRef:
            name: {{ include "pydici.fullname" . }}-django
        resources:
          {{- toYaml .Values.celery.beat.resources | nindent 10 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}